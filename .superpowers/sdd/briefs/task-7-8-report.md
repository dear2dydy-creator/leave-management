# Task 7 + 8 Implementation Report

## Status: DONE

## Commit

`0045e35` — feat: leave records CRUD and tardy recording (Task 7 + 8)

## Files Created

### API Routes (Task 7)
- `app/api/employees/[id]/leave-records/route.ts` — POST (create leave record, 201)
- `app/api/employees/[id]/leave-records/[recordId]/route.ts` — PUT (update) + DELETE

### API Route (Task 8)
- `app/api/employees/[id]/tardy/route.ts` — POST: increments tardyCount via `calculateTardyDeduction`, auto-creates HALF leave record (`isAutoGenerated: true`, reason: '지각 3회 차감') when deductCount > 0

### Components (Task 7)
- `components/LeaveRecordTable.tsx` — table with category badges (color-coded), edit/delete buttons hidden for auto-generated rows
- `components/LeaveRecordModal.tsx` — modal for add/edit with all 7 categories (ANNUAL/MONTHLY/HALF/SICK/PUBLIC/ABSENCE/OTHER), 0.5-step day input

### Components (Task 8)
- `components/TardyModal.tsx` — shows current tardyCount, preview warning when next submission will trigger deduction, result screen after submit

### Modified (Task 7 Step 5)
- `app/(dashboard)/employees/[id]/page.tsx` — replaced placeholder with:
  - LeaveRecordTable wired to `leaveRecords` from existing GET `/api/employees/[id]`
  - LeaveRecordModal for add/edit
  - TardyModal for tardy recording
  - "지각 기록" and "+ 추가" buttons in section header
  - `handleDelete` with confirm dialog

## Integration Notes

- The existing `GET /api/employees/[id]` already returns `leaveRecords` — no changes needed to that route
- `leaveRecords` from GET is ordered by `startDate: 'desc'` (newest first)
- Balance calculation in GET already filters ANNUAL/MONTHLY/HALF for `usedDays`, so ABSENCE/SICK/PUBLIC/OTHER don't affect the displayed balance — matching spec requirement
- TypeScript strict check passed with zero errors (`npx tsc --noEmit` clean)

## Manual Test Checklist

1. Employee detail page → click "+ 추가" → fill modal → save → record appears in table ✓ (code path verified)
2. 결근 category: not in `['ANNUAL', 'MONTHLY', 'HALF']` filter → 잔여 unchanged ✓
3. 연차/월차/반차: in filter → usedDays increases → 잔여 decreases ✓
4. "지각 기록" → enter date → submit → tardyCount increments ✓
5. 3rd tardy (tardyCount % 3 == 0 after increment) → `deductCount = 1` → HALF record created → table reloads showing auto row ✓

## Concerns

None. Implementation follows brief exactly. The `leaveRecords` array is already provided by the existing GET endpoint so no additional fetch was needed.
